- Don't send "here are the recipes we found" if we didn't find any
- Show the recipe after adding, or updating
- Add a "show recipe" command
  - Maybe do this in the prompt?
  - Show Tool Use (messages for update/add recipes, etc.) ?

- Investigate submitting websites / urls
- Investigate submitting images / camera photos
- Investigate audio

- Let the users know when we looked for recipes but found none, so the answer is just GPT yoloing it

- After the first message consider letting it determine whether to ask for relevant recipes? Good chance it already has the important ones, right?

- Type hints
- Black working
